# Tänd ett ljus

A 3D interactive candle visualization on a map of Sweden. Users can "light a candle" at any Swedish city, and watch it glow on the map.

## Quick Start

```bash
npm install
npm run dev
```

Then open http://localhost:3000
